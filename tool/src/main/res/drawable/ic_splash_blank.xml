<?xml version="1.0" encoding="utf-8"?>
<!--
    Solid black square, matching the splash background exactly, rather than a
    literally empty drawable. An empty vector (no <path> elements at all) appears
    to trip a fallback in the platform SplashScreen implementation - when it finds
    no valid icon content, it falls back toward the app's launcher icon, which
    doesn't exist in our manifest either (see ManifestGenerator - it never emits
    android:icon), so it cascades down to a generic system default. A real but
    all-black drawable passes that validity check while staying visually invisible
    against the equally-black splash background, regardless of any icon-shape
    masking the OS applies around it.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M0,0h108v108h-108z"
        android:fillColor="#000000" />
</vector>
